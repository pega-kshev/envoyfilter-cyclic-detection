apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-services.fullname" . }}-test-connection"
  labels:
    {{- include "test-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
